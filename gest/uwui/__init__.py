"""urwid frontend for GeST (in-progress migration from Textual).

Only this package is framework-specific; it renders the same frontend-agnostic
``gest.core`` modules the Textual frontend does. See the migration notes in the
project README / memory. Entry point: ``gest-uw`` (``gest.uwui.app:main``).
"""
